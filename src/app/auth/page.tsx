import { Login } from "@/components/pages/auth/login";

export default function LoginPage() {
    return (
        <main className="max-w-full h-screen max-h-screen flex items-center justify-center">
            <Login />
        </main>
    );
}
